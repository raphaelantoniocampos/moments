import 'package:flutter/material.dart';

class NewProfilePicture extends StatelessWidget {
  const NewProfilePicture({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(),);
  }
}
